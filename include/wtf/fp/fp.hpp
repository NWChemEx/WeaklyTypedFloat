#pragma once
#include <wtf/fp/float.hpp>
#include <wtf/fp/float_view.hpp>

/** @brief Contains classes and functions pertaining to the Float class
 *         hierarchy.
 *
 *  `float` is a protected keyword in C++, so we use `fp` (floating point)
 *  instead.
 */
namespace wtf::fp {}